<% provide(:title, 'All users') %>
<h1>All users</h1>
<%= will_paginate %>
<ul class="users">
	<li><%= link_to "New User", newuser_path %></li>
  <% @users.each do |user| %>
  <%= render @users %>
  <% end %>
</ul>
<%= will_paginate %>